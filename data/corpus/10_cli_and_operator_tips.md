# CLI and Operator Tips

Run `lab doctor` first to check the environment, Ollama connectivity, and installed models. Pull only the models you need to save storage.

Use `lab models recommend` to pick sensible defaults for chat, RAG QA, and embeddings based on the policy file.

